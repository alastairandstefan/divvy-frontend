const GroupCard = ({groupName}) => {
    console.log(groupName);
    return (
  
        <div className="">
          <h1>{groupName}</h1>
        </div>
      
    );
  };
  
  export default GroupCard;
  